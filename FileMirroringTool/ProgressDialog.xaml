<Window x:Class="FileMirroringTool.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileMirroringTool"
        mc:Ignorable="d"
        Title="{Binding PrgTitle}" Height="110" Width="500">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="10">
            <ProgressBar Minimum="0" Maximum="100" Value="{Binding PrgVal}"/>
            <TextBlock Text="{Binding PrgStr}" TextAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
